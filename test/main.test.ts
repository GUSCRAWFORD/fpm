

import 'mocha';
import { equal } from 'assert';
import { InstallCommand } from '../src/install-command';
import { TransformCommand } from '../src/transform-command';
//const USERS_ROUTE = 'users';
describe(`⚙️  Core Configs`, function (){
    //console.info(routes);
    let install = new InstallCommand(), transform = new TransformCommand();
    it(`🔬 Unit Tests Work`, function () {
        equal(
            true
            , true
        );
    });
});